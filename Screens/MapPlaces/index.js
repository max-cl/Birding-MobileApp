import React from "react";

// Container
import MapPlacesContainer from "../../components/Containers/MapPlaces";

const MapPlacesScreen = ({ route, navigation }) => {
    return <MapPlacesContainer route={route} navigation={navigation} />;
};

export default MapPlacesScreen;
