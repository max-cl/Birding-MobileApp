import React from "react";

// Container
import PlaceDetailsContainer from "../../components/Containers/PlaceDetails";

const PlaceDetailsScreen = ({ route }) => {
    return <PlaceDetailsContainer route={route} />;
};

export default PlaceDetailsScreen;
